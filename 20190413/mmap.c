#include <headFile.h>

int main(int argc,char *argv[])
{
	//创建文件接收标志
	/*
	   打开文件
	   打印文件标志符
	   创建字符指针
	   创建stat结构体
	   读取文件信息 fstat
	   使用mmap
	   查看mmap返回的参数是否正确
	   拷贝需要的字符到文件里面
	   关闭mmap
	   返回
	   */
}
